import Menu from '../Menu/Menu'

const Home = ({ history }) => {
  return (
    <div>
      <h1>Cronos</h1>
      <h2>English Practice</h2>

      <Menu history={history} />
    </div>
  )
}

export default Home
